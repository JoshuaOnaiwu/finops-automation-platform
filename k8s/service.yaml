apiVersion: v1
kind: Service

metadata:
  name: finops-service
  namespace: finops

spec:
  selector:
    app: finops-platform

  ports:
    - protocol: TCP
      port: 80
      targetPort: 4000

  type: LoadBalancer